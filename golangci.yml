version: "2"

run:
  timeout: 5m

linters:
  default: standard
  enable:
    - errcheck
    - errname
    - errorlint
    - goconst
    - gocritic
    - misspell
    - unconvert
  exclusions:
    rules:
      - path: '_test\.go'
        linters:
          - errcheck
